describe('b_crashes', () => {
  it('crashes the browser', () => {
    // cy.then(() => {
    //   Cypress.automation('remote:debugger:protocol', { command: 'Browser.close' })
    // }).wait(10000)
  })
})